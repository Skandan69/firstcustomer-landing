import{LOCAL_BUSINESS_CONFIG}from'./configuration.mjs';
import{workspaceId}from'./persistence-service.mjs';
let activeController=null;
function track(event,properties){try{window.analytics?.track(event,properties);}catch{}}
export class BusinessSearchError extends Error{constructor(message,code='SERVICE_ERROR',detail=''){super(message);this.name='BusinessSearchError';this.code=code;this.detail=detail;}}
export async function searchLocalBusinesses(criteria){if(activeController)activeController.abort();const controller=new AbortController();activeController=controller;const timeout=setTimeout(()=>controller.abort(),LOCAL_BUSINESS_CONFIG.requestTimeoutMs);try{track('business_search',{provider:criteria.provider||'auto',radius_km:Number(criteria.radiusKm)||0,category_provided:Boolean(criteria.category)});const response=await fetch(LOCAL_BUSINESS_CONFIG.endpoint,{method:'POST',headers:{'Content-Type':'application/json','X-Workspace-Id':workspaceId()},body:JSON.stringify(criteria),signal:controller.signal});const payload=await response.json().catch(()=>({}));if(!response.ok)throw new BusinessSearchError(payload.error?.message||'The business search service is unavailable.',payload.error?.code||'SEARCH_FAILED');return{businesses:Array.isArray(payload.businesses)?payload.businesses:[],nextPageToken:payload.nextPageToken||null,provider:payload.provider||null,notice:payload.notice||'',fallback:payload.fallback||null};}catch(error){if(error instanceof BusinessSearchError)throw error;if(error.name==='AbortError')throw new BusinessSearchError('The search was cancelled or timed out.','TIMEOUT');throw new BusinessSearchError('We could not reach the business search service.','NETWORK_ERROR',error.message);}finally{clearTimeout(timeout);if(activeController===controller)activeController=null;}}

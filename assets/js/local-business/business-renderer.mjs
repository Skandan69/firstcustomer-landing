const escapeHtml=(value='')=>String(value).replace(/[&<>'"]/g,(char)=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));
const safeUrl=(value='')=>{try{const url=new URL(value);return['http:','https:'].includes(url.protocol)?url.href:'';}catch{return'';}};
const phoneDigits=(value='')=>String(value).replace(/\D/g,'');
export function renderBusinessCard(business){const website=safeUrl(business.website);const sourceUrl=safeUrl(business.sourceUrl);const phone=business.internationalPhone||business.phone;const digits=phoneDigits(phone);const sourceLabel=business.source==='google'?'Google Places':'OpenStreetMap';const rating=business.rating===null?'Not available':`${escapeHtml(business.rating)} ★`;const reviews=business.reviewCount===null?'Not available':`${escapeHtml(business.reviewCount)} reviews`;return`<article class="lbf-card"><div class="lbf-card-head"><div><h3>${escapeHtml(business.name||'Unnamed business')}</h3><div class="lbf-category">${escapeHtml(business.category||'Local business')}</div></div><div class="lbf-rating">${rating} · ${reviews}</div></div><div class="lbf-meta"><div>${escapeHtml(business.address||'Address unavailable')}</div><div>${phone?escapeHtml(phone):'No public phone'}</div><div>${website?`<a href="${escapeHtml(website)}" target="_blank" rel="noopener">${escapeHtml(business.website)}</a>`:'No website'}</div>${business.openingHours?`<div>Opening hours: ${escapeHtml(business.openingHours)}</div>`:''}</div><div class="lbf-badges"><span class="lbf-badge lbf-source-badge">${sourceLabel}</span><span class="lbf-badge ${website?'unknown':'no-website'}">${website?'Website Available — Not Audited':'No Website'}</span>${business.businessStatus?`<span class="lbf-badge operational">${escapeHtml(formatStatus(business.businessStatus))}</span>`:''}</div><div class="lbf-actions">${sourceUrl?`<a class="lbf-action primary" href="${escapeHtml(sourceUrl)}" target="_blank" rel="noopener">View source</a>`:''}${digits?`<a class="lbf-action" href="tel:${digits}">Call</a><a class="lbf-action" href="https://wa.me/${digits}" target="_blank" rel="noopener">WhatsApp</a>`:''}<button class="lbf-action" disabled title="Coming next">Save Lead · Coming next</button><button class="lbf-action" disabled title="Coming next">Audit Website</button></div></article>`;}
export function renderState(type,title,message){return`<div class="lbf-state">${type==='loading'?'<div class="lbf-spinner"></div>':'<div class="lbf-state-icon">⌖</div>'}<h3>${escapeHtml(title)}</h3><p>${escapeHtml(message)}</p></div>`;}function formatStatus(value){return String(value).toLowerCase().replace(/_/g,' ').replace(/^./,(c)=>c.toUpperCase());}

const{recordSearch}=require('./repository');
module.exports=async function(req,res){if(process.env.VERCEL_ENV!=='preview')return res.status(404).json({error:{code:'NOT_FOUND',message:'Not found.'}});if(req.method!=='POST')return res.status(405).json({error:{code:'METHOD_NOT_ALLOWED',message:'Use POST.'}});const workspaceId=String(req.headers?.['x-workspace-id']||'');if(!/^[0-9a-f-]{36}$/i.test(workspaceId))return res.status(400).json({error:{code:'INVALID_WORKSPACE',message:'Invalid workspace.'}});try{const rows=await recordSearch(workspaceId,{location:'Persistence Validation',category:'Test',radiusKm:2,provider:'open_data'},{provider:'open_data',businesses:[{}]});return res.status(201).json({item:rows?.[0]||null});}catch(error){return res.status(error.status||500).json({error:{code:error.code||'PERSISTENCE_FAILED',message:error.message}});}};
